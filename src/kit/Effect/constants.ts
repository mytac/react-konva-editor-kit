const BLACK = '#000000';
const WHITE = '#ffffff';
const TRANSPARENT = '#ffffff00';

const OPACITY_VALID = {
  image: ['shadowColor', 'fill'],
  font: ['shadowColor', 'fill'],
};

export { BLACK, WHITE, OPACITY_VALID, TRANSPARENT };
